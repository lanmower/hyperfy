// Minimal meadow app script
export default function setup(app) {
  // Empty meadow setup - no app logic needed
  // The scene rendering is handled by the scene blueprint
  return {
    dispose: () => {},
  }
}
